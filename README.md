# web3 transaction fetch

### start app
```
node -r ts-node/register/transpile-only app/main.ts -f 0 -l 1000
```

### start app with multi process
```
node -r ts-node/register/transpile-only app/main.ts -f 0 -l 1000 -m 4
```